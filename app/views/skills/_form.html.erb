<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_type %><br>
    <%= f.text_field :skill_type %>
  </div>
  <div class="field">
    <%= f.label :skill_name %><br>
    <%= f.text_field :skill_name %>
  </div>
  <div class="field">
    <%= f.label :skill_code %><br>
    <%= f.text_field :skill_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
