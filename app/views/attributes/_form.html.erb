<%= form_for(@attribute) do |f| %>
  <% if @attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
      <% @attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attribute_type %><br>
    <%= f.text_field :attribute_type %>
  </div>
  <div class="field">
    <%= f.label :attribute_key %><br>
    <%= f.text_field :attribute_key %>
  </div>
  <div class="field">
    <%= f.label :attribute_value %><br>
    <%= f.text_field :attribute_value %>
  </div>
  <div class="field">
    <%= f.label :active_status %><br>
    <%= f.text_field :active_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
